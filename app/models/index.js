const USERS = require('./users')
const GROUPS = require('./groups')
const MESSAGES = require('./messages')
const USERS_CONTACTS = require('./usersContacts')
const USERS_GROUPS = require('./usersGroups')

module.exports = {
    USERS,
    GROUPS,
    MESSAGES,
    USERS_CONTACTS,
    USERS_GROUPS,
}
